# strategy/ — 🔒 策略模組（專有，未公開）

本目錄的策略實作**刻意未包含在公開版本中**。

以下檔案為專有內容，已從公開追蹤移除（本機仍保留，供作者自行運行）：

| 檔案 | 內容（已隱去） |
|------|---------------|
| `detector.py` | 進場型態偵測條件 |
| `fibonacci.py` | Fibonacci 價位比例與計算 |
| `state_machine.py` | 策略狀態機（進場／失效／出場判定） |
| `candle_builder.py` | 多週期 K 棒合成 |

> **為什麼是空白？**
> 進場訊號、價位比例與分批出場規則是本專案的核心，屬專有內容。
> 系統其餘部分——交易所抽象層、adapter、風控框架、執行器、測試與部署——皆完整公開，
> 詳見根目錄 `README.md` 與 `急速框交易機器人_技術文件.md`（架構篇）。

若要實際運行，需自行在本目錄補上上述模組，並實作 `strategy/__init__.py` 所預期的介面。
